import { tool } from "@opencode-ai/plugin";

export const {{name}}Tool = tool({
    description: "{{description}}",
    {{{argsSchema}}},
    async execute(args) {
        {{implementation}}
    },
});
